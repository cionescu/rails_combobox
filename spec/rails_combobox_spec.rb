require 'spec_helper'

describe RailsCombobox do
  it 'has a version number' do
    expect(RailsCombobox::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
